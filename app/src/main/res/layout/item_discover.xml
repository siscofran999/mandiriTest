<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        
        <variable
            name="discover"
            type="com.siscofran.mandiri.data.model.Result" />
        
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <ImageView
            android:id="@+id/img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:imageUrl="@{discover.poster_path}"
            android:src="@drawable/ic_launcher_background"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <TextView
            android:id="@+id/txv_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            tools:text="Harry Potter"
            android:textColor="@color/black2"
            android:text="@{discover.title}"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginStart="8dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@+id/img"
            app:layout_constraintEnd_toEndOf="parent"/>

        <TextView
            android:id="@+id/txv_overview"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            tools:text="Harry Potter"
            android:maxLines="3"
            android:ellipsize="end"
            android:text="@{discover.overview}"
            android:lines="3"
            app:layout_constraintTop_toBottomOf="@+id/txv_title"
            app:layout_constraintStart_toStartOf="@+id/txv_title"
            app:layout_constraintEnd_toEndOf="parent"/>

        <TextView
            android:id="@+id/txv_release"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="31-01-1999"
            android:text="@{discover.release_date}"
            app:layout_constraintBottom_toBottomOf="@+id/img"
            app:layout_constraintStart_toStartOf="@+id/txv_title" />

    </androidx.constraintlayout.widget.ConstraintLayout>
    
</layout>